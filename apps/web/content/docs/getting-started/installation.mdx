---
title: Installation
description: Install MangoWC and required dependencies
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Dependencies

Required system dependencies:

- glibc, wayland, wayland-protocols
- libinput, libdrm, libxkbcommon, pixman
- git, meson, ninja
- libdisplay-info, libliftoff, hwdata
- seatd, pcre2, xorg-xwayland, libxcb

## Installation

<Tabs items={['Arch Linux', 'Gentoo Linux', 'Other']}>
<Tab>

Available via AUR:

```bash
yay -S mangowc-git
```

</Tab>
<Tab>

Add GURU repository:

```bash
eselect repository enable guru
emerge --sync guru
```

Add to package.accept_keywords, then install:

```bash
emerge --ask gui-wm/mangowc
```

</Tab>
<Tab>

Build from source:

```bash
# Build wlroots
git clone -b 0.19.2 https://gitlab.freedesktop.org/wlroots/wlroots.git
cd wlroots
meson build -Dprefix=/usr
sudo ninja -C build install

# Build scenefx
git clone -b 0.4.1 https://github.com/wlrfx/scenefx.git
cd scenefx
meson build -Dprefix=/usr
sudo ninja -C build install

# Build MangoWC
git clone https://github.com/DreamMaoMao/mangowc.git
cd mangowc
meson build -Dprefix=/usr
sudo ninja -C build install
```

</Tab>
</Tabs>

<Callout type="info">
  For NixOS users, see the [integrations](/docs/features/integrations) page.
</Callout>

## Suggested Tools

| Category | Options |
|----------|---------|
| Launcher | rofi, bemenu, wmenu, fuzzel |
| Terminal | foot, wezterm, alacritty, kitty |
| Status Bar | waybar, eww, quickshell, ags |
| Wallpaper | swww, swaybg |
| Notifications | swaync, dunst, mako |
| Desktop Portal | xdg-desktop-portal-wlr, xdg-desktop-portal-gtk |
| Clipboard | wl-clipboard, wl-clip-persist, cliphist |
| Gamma Control | wlsunset, gammastep |
| Utilities | xfce-polkit, wlogout |

## Default Keybindings

Common defaults after installation:

- `Alt+Return`: Open terminal (foot)
- `Alt+Space`: Open launcher (rofi)
- `Alt+Q`: Kill focused window
- `Alt+Left/Right/Up/Down`: Focus direction
- `Super+M`: Quit MangoWC